# 2026-03-08 优化日志

## Task 1
- 需求/优化：初始化项目依赖与应用入口
- 目的：建立 FastAPI 项目最小可运行骨架，为后续博客开发提供统一入口与基础配置
- 模块：`requirements.txt`、`.env.example`、`app/main.py`、`app/core/config.py`、`app/core/db.py`、`tests/test_health.py`
- 验证标准：`/health` 可访问并返回 `{"status": "ok"}`；健康检查测试通过

## Task 2
- 需求/优化：建立数据库模型与基础映射
- 目的：定义博客核心数据结构，为文章、分类、标签与管理员功能提供持久化基础
- 模块：`app/models/__init__.py`、`app/models/user.py`、`app/models/category.py`、`app/models/tag.py`、`app/models/post.py`、`tests/test_models.py`
- 验证标准：核心模型可导入；`users`、`categories`、`tags`、`posts`、`post_tags` 元数据存在；模型测试通过

## Task 3
- 需求/优化：添加 slug 生成逻辑与基础 service
- 目的：为公开 URL 提供统一、稳定且可唯一化的标识生成能力
- 模块：`app/services/post_service.py`、`tests/test_post_service.py`
- 验证标准：slug 可从标题生成；重复 slug 可追加数字后缀；相关测试通过

## Task 4
- 需求/优化：配置 pytest 测试基座
- 目的：建立可复用的测试客户端与测试数据库隔离环境，支撑后续路由与业务测试
- 模块：`tests/conftest.py`、`tests/test_fixtures.py`、`app/core/db.py`
- 验证标准：`client` fixture 可用；测试数据库可初始化；基础 fixture smoke test 通过

## Task 5
- 需求/优化：实现密码哈希与 session 鉴权基础设施
- 目的：提供管理员认证所需的密码加密、校验与鉴权依赖能力
- 模块：`app/core/security.py`、`app/core/deps.py`、`app/schemas/auth.py`、`app/services/auth_service.py`、`requirements.txt`、`tests/test_admin_auth.py`
- 验证标准：密码可安全哈希并验证；管理员认证基础逻辑可用；认证相关测试通过

## Task 6
- 需求/优化：实现管理员登录页与登录/登出路由
- 目的：打通后台最小认证闭环，为后续文章后台管理提供入口
- 模块：`app/routes/admin_auth.py`、`app/templates/admin/login.html`、`app/main.py`、`tests/test_admin_auth.py`、`tests/conftest.py`
- 验证标准：登录页可访问；登录成功可写入 session；登出可清除 session；相关测试通过

## Task 7
- 需求/优化：初始化模板基座与公共样式
- 目的：建立前台 SSR 页面基础结构，为首页、详情页、分类页、标签页提供统一布局与静态资源能力
- 模块：`app/routes/public.py`、`app/templates/base.html`、`app/templates/public/index.html`、`app/static/css/style.css`、`app/static/css/admin.css`、`app/main.py`、`tests/test_public_pages.py`
- 验证标准：首页 `/` 可返回 HTML；静态资源已挂载；首页 HTML 测试通过

## Task 8
- 需求/优化：实现前台首页文章列表页
- 目的：让博客首页具备文章展示能力，形成前台内容入口
- 模块：`app/services/post_service.py`、`app/routes/public.py`、`app/templates/public/index.html`、`tests/conftest.py`、`tests/test_public_pages.py`
- 验证标准：首页可显示文章标题；无文章时显示“暂无文章”；相关测试通过

## Task 9
- 需求/优化：实现文章详情页
- 目的：支持通过 slug 访问单篇文章，形成博客内容阅读页
- 模块：`app/services/post_service.py`、`app/routes/public.py`、`app/templates/public/post_detail.html`、`tests/test_public_pages.py`
- 验证标准：`/posts/{slug}` 可正常展示文章；不存在的 slug 返回 404；相关测试通过

## Task 10
- 需求/优化：实现分类页与标签页
- 目的：支持按分类与标签组织文章内容，完善博客前台导航结构
- 模块：`app/services/taxonomy_service.py`、`app/routes/public.py`、`app/templates/public/category_detail.html`、`app/templates/public/tag_detail.html`、`tests/test_public_pages.py`
- 验证标准：`/categories/{slug}` 与 `/tags/{slug}` 可展示对应文章列表；相关测试通过

## Task 11
- 需求/优化：定义文章表单 schema 与后台创建文章逻辑
- 目的：为后台文章创建提供统一输入结构和 slug 生成逻辑，支撑后续页面与提交流程
- 模块：`app/schemas/post.py`、`app/services/post_service.py`、`tests/test_admin_posts.py`
- 验证标准：`build_post` 可基于标题生成唯一 slug；文章创建基础行为测试通过

## Task 12
- 需求/优化：实现后台文章列表页
- 目的：为管理员提供文章管理入口，支持登录后查看已有文章并进入创建/编辑流程
- 模块：`app/routes/admin_posts.py`、`app/templates/admin/post_list.html`、`app/main.py`、`tests/test_admin_posts.py`
- 验证标准：未登录访问 `/admin/posts` 会跳转登录页；登录后可看到文章管理页与文章列表

## Task 13
- 需求/优化：实现后台新建文章页与提交逻辑
- 目的：打通后台文章创建闭环，让管理员能通过表单创建并保存文章
- 模块：`app/routes/admin_posts.py`、`app/templates/admin/post_form.html`、`tests/conftest.py`、`tests/test_admin_posts.py`
- 验证标准：新建页可展示分类与标签；提交后成功跳转列表页；新建文章及中文 slug 可在后台列表中看到

## Task 14
- 需求/优化：实现后台编辑文章页与提交逻辑
- 目的：补齐后台文章管理闭环，让管理员可回显并更新已有文章，同时保持已发布链接稳定
- 模块：`app/routes/admin_posts.py`、`app/services/post_service.py`、`app/templates/admin/post_form.html`、`tests/test_admin_posts.py`
- 验证标准：编辑页可回显原文章数据；提交后标题/摘要/正文可更新；slug 保持不变；相关测试通过

## Task 15
- 需求/优化：增加初始化管理员脚本
- 目的：提供首个管理员账户创建入口，避免手工写库，支撑新环境初始化
- 模块：`scripts/create_admin.py`、`app/services/auth_service.py`、`tests/test_admin_auth.py`
- 验证标准：管理员构建逻辑可复用；脚本文件存在并可作为初始化入口；相关测试通过

## Task 16
- 需求/优化：引入 Alembic 并固化首版迁移
- 目的：让博客数据库结构具备可追踪、可迁移的初始化基线，方便后续演进
- 模块：`alembic.ini`、`migrations/env.py`、`migrations/versions/20260308_init_blog_tables.py`、`tests/conftest.py`
- 验证标准：迁移配置已建立；首版表结构迁移文件已落地；博客元数据完整性测试通过

## Task 17
- 需求/优化：完整回归测试与手工验收
- 目的：确认博客 MVP 在测试、后台流程与前台页面层面形成可验收闭环
- 模块：`tests/test_admin_posts.py`、`tests/test_admin_auth.py`、`tests/test_public_pages.py`、`tests/conftest.py`、`project_logs/manual_checks/task17_web_check_8001.py`
- 验证标准：完整 pytest 回归通过；后台登录/新建/编辑流程可跑通；前台首页可访问并可见验收文章

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-11 11:52:00

**负责人**：Claude

---

#### 变更目的
完成 P3 整改任务，在不引入额外状态管理和不做审美式重构的前提下，控制后端 `api_v1_posts.py` 与前端 `AdminPostsPage.jsx` 的复杂度继续增长。

#### 变更内容
- **具体修改**：
  - 后端按职责最小拆分出 `api_v1_taxonomy.py` 与 `api_v1_media.py`，将 taxonomy 与 media 上传从 `api_v1_posts.py` 中分离。
  - `api_v1_posts.py` 收敛为以文章读写、导入导出、发布流转为核心的路由文件。
  - 前端新增 `AdminPostEditor.jsx` 与 `AdminPostList.jsx`，把 `AdminPostsPage.jsx` 中最拥挤的编辑区和列表区拆为子组件，页面继续保留状态与调度逻辑。
  - `app/main.py` 同步注册新增路由；整改文档中将 P3-1 标记为已完成。
- **涉及模块**：`app/routes/api_v1_posts.py`、`app/routes/api_v1_taxonomy.py`、`app/routes/api_v1_media.py`、`app/main.py`、`frontend/src/admin/pages/AdminPostsPage.jsx`、`frontend/src/admin/components/AdminPostEditor.jsx`、`frontend/src/admin/components/AdminPostList.jsx`、`docs/implementation-alignment-remediation-checklist.md`
- **实施方法**：采用最小拆分策略，仅抽离边界清晰的 taxonomy/media 路由与后台编辑子视图，不新增全局 store、不改变 API 契约、不扩大页面状态层级。

#### 验证标准
- **成功指标**：后端职责边界更清晰、前端文章页更易维护、全量测试与前端构建继续通过。
- **测试方案**：运行 `tests/test_api_v1_posts.py` 与完整 `tests/` 测试集，并执行前端 `npm run build`。
- **风险评估**：中低。拆分涉及路由注册与前端组件引用，存在导入路径或契约回退风险；已通过全量回归测试与构建验证覆盖。

#### 后续建议
- **相关影响**：当前已完成最小复杂度收口，后续若后台文章页再扩功能，可继续优先保持“页面调度 + 子组件展示”的结构，不要直接把逻辑重新塞回页面本体。
- **待办事项**：若后续 taxonomy 能力继续增长，可进一步把查询与写入逻辑向 service 层收口，但应继续避免过度抽象。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 16:20:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 1，收口开发态前后端联调约定，明确推荐启动入口、Vite 代理策略、端口与静态资源访问方式，降低开发环境行为不一致带来的排查成本。

#### 变更内容
- **具体修改**：新增 `docs/dev-fullstack-contract.md`，系统说明 `start_fullstack.py` 启动约定、默认端口、Vite 代理策略、静态资源访问方式、登录相关行为与开发排查建议；同时在 `docs/next-phase-optimization-todos.md` 中将 TODO 1 标记为已完成并关联文档。
- **涉及模块**：`docs/dev-fullstack-contract.md`、`docs/next-phase-optimization-todos.md`
- **实施方法**：不引入新代码路径，仅对现有联调实现进行文档化收口，确保开发约定与当前实现保持一致。

#### 验证标准
- **成功指标**：开发态联调约定被正式文档化，TODO 1 在计划文档中完成标记。
- **测试方案**：运行 `tests/test_start_fullstack_script.py`，确认联调启动脚本相关行为仍通过。
- **风险评估**：低。此次主要是文档收口，不改变既有运行逻辑。

#### 后续建议
- **相关影响**：后续执行 TODO 2、TODO 3 时，可直接引用该文档作为开发态行为基线。
- **待办事项**：在浏览器冒烟测试稳定后，可将关键测试命令补充到该文档中。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 16:45:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 2，补齐关键浏览器冒烟测试，用少量高价值流程覆盖登录、后台入口、发文、发布和前台查看文章详情，提升用户路径层面的回归保障。

#### 变更内容
- **具体修改**：新增 `project_logs/manual_checks/browser_smoke_test.py` 与结果文件 `browser_smoke_result.txt`，覆盖登录、登录后访问 `/admin/login` 自动回跳、登录后从首页点击“后台”进入 `/admin`、新建文章、发布文章、前台查看文章详情；同时在 `docs/next-phase-optimization-todos.md` 中将 TODO 2 标记为已完成。
- **涉及模块**：`project_logs/manual_checks/browser_smoke_test.py`、`project_logs/manual_checks/browser_smoke_result.txt`、`docs/next-phase-optimization-todos.md`
- **实施方法**：沿用 `scripts/with_server.py` 作为浏览器测试启动管理器，通过单个 Playwright 脚本覆盖关键用户路径，再配合后端回归测试共同验证。

#### 验证标准
- **成功指标**：6 条关键浏览器链路全部通过，且对应后端回归测试通过。
- **测试方案**：运行 `project_logs/manual_checks/browser_smoke_test.py`，并执行 `tests/test_api_v1_auth.py`、`tests/test_api_v1_posts.py`、`tests/test_start_fullstack_script.py`。
- **风险评估**：中低。浏览器冒烟测试引入了临时测试数据库与脚本，但未改变业务逻辑；需在任务结束后清理临时测试产物。

#### 后续建议
- **相关影响**：后续执行 TODO 3、TODO 5 时，可直接复用该脚本作为关键路径回归基线。
- **待办事项**：在 TODO 6 中统一临时测试脚本与结果文件的归档/清理规则。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 17:05:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 3，统一后台入口行为，确保未登录用户进入登录页、已登录用户进入后台首页，并保证已登录访问登录页时自动回跳。

#### 变更内容
- **具体修改**：基于现有实现补充了后台入口行为的浏览器验证脚本 `todo3_admin_entry_test.py` 与结果文件 `todo3_admin_entry_result.txt`，覆盖匿名后台入口、登录后后台入口与登录页自动回跳三类行为；同时在 `docs/next-phase-optimization-todos.md` 中将 TODO 3 标记为已完成。
- **涉及模块**：`project_logs/manual_checks/todo3_admin_entry_test.py`、`project_logs/manual_checks/todo3_admin_entry_result.txt`、`docs/next-phase-optimization-todos.md`
- **实施方法**：沿用当前 PublicLayout / LoginPage / AuthGuard 实现，不新增逻辑分叉，而是用浏览器级验证确认三处后台入口行为已经一致。

#### 验证标准
- **成功指标**：匿名后台入口、登录后后台入口、已登录访问登录页三条行为全部通过。
- **测试方案**：运行 `todo3_admin_entry_test.py`，并执行 `tests/test_api_v1_auth.py` 与 `tests/test_start_fullstack_script.py`。
- **风险评估**：低。此次主要是行为收口验证和计划状态更新，不改变核心协议。

#### 后续建议
- **相关影响**：后续执行 TODO 5 时，可直接沿用本次后台入口行为基线，避免再次出现“入口与登录态不一致”的问题。
- **待办事项**：在 TODO 6 中统一这类临时浏览器验证脚本的归档或清理规则。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 17:25:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 4，以最小必要改动继续收口后台内容管理复杂度，避免 `api_v1_posts.py` 与 `AdminPostsPage.jsx` 继续膨胀。

#### 变更内容
- **具体修改**：
  - 后端新增 `app/services/admin_post_service.py`，抽离默认分类解析与标签批量查询逻辑，减少 `api_v1_posts.py` 中的重复查询与职责堆积。
  - `api_v1_posts.py` 改为复用 `resolve_category_id()` 与 `get_tags_by_ids()`，保留文章主流程路由本体不变。
  - 前端新增 `frontend/src/admin/components/AdminPostFilters.jsx`，将筛选区从 `AdminPostsPage.jsx` 中拆出，使页面继续聚焦状态管理与调度。
  - `api_v1_taxonomy.py` 复用新的存在性判断辅助逻辑，减少重复 slug 冲突校验实现。
  - 在 `docs/next-phase-optimization-todos.md` 中将 TODO 4 标记为已完成。
- **涉及模块**：`app/services/admin_post_service.py`、`app/routes/api_v1_posts.py`、`app/routes/api_v1_taxonomy.py`、`frontend/src/admin/components/AdminPostFilters.jsx`、`frontend/src/admin/pages/AdminPostsPage.jsx`、`docs/next-phase-optimization-todos.md`
- **实施方法**：坚持最小拆分，只抽离重复逻辑与独立筛选视图，不新增全局抽象层，不改变现有 API 契约与页面状态结构。

#### 验证标准
- **成功指标**：后台内容管理热点文件复杂度进一步下降，回归测试和前端构建继续通过。
- **测试方案**：运行 `tests/test_api_v1_posts.py`、`tests/test_start_fullstack_script.py`，并执行前端 `npm run build`。
- **风险评估**：低到中。主要风险是抽取辅助函数后出现导入或行为回退；已通过接口测试与前端构建验证覆盖。

#### 后续建议
- **相关影响**：后续如果后台文章页再扩功能，可继续优先拆分独立视图片区块，而不是往 `AdminPostsPage.jsx` 继续堆积。
- **待办事项**：TODO 5 可基于当前统一入口与登录态逻辑，继续收口前端认证体验细节。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 17:40:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 5，统一前端认证体验细节，确保登录、刷新、登录页回跳、登出与后台入口在同一登录态判断下行为一致。

#### 变更内容
- **具体修改**：
  - 在 `frontend/src/shared/auth/session.js` 中新增 `hasStoredUser()`，统一前端登录态存在性判断入口。
  - `PublicLayout.jsx`、`AuthGuard.jsx`、`LoginPage.jsx` 统一改为复用该函数，避免各处散写本地认证判断逻辑。
  - 新增 `project_logs/manual_checks/todo5_auth_experience_test.py` 与结果文件 `todo5_auth_experience_result.txt`，覆盖登录、刷新、已登录访问登录页、登出、登出后后台入口行为。
  - 在 `docs/next-phase-optimization-todos.md` 中将 TODO 5 标记为已完成。
- **涉及模块**：`frontend/src/shared/auth/session.js`、`frontend/src/shared/layout/PublicLayout.jsx`、`frontend/src/shared/auth/AuthGuard.jsx`、`frontend/src/admin/pages/LoginPage.jsx`、`project_logs/manual_checks/todo5_auth_experience_test.py`、`project_logs/manual_checks/todo5_auth_experience_result.txt`、`docs/next-phase-optimization-todos.md`
- **实施方法**：保持现有 session + localStorage 方案不变，仅收口前端登录态判断入口，再用浏览器级验证确认交互体验一致。

#### 验证标准
- **成功指标**：登录后进入后台、刷新保持登录态、登录页自动回跳、登出回登录页、登出后首页后台入口恢复未登录状态，五项行为全部通过。
- **测试方案**：运行 `todo5_auth_experience_test.py`，并执行 `tests/test_api_v1_auth.py` 与 `tests/test_start_fullstack_script.py`。
- **风险评估**：低。改动聚焦前端认证体验判断，不改变后端协议和真实认证边界。

#### 后续建议
- **相关影响**：后续若需要扩展更多后台入口或认证相关页面，优先复用 `hasStoredUser()` 作为前端存在性判断基线。
- **待办事项**：TODO 6 中统一这类临时浏览器验证脚本的归档或清理规则。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 17:55:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 6，整理调试/临时脚本管理方式，降低一次性排查文件长期堆积、误提交和误阅读的风险。

#### 变更内容
- **具体修改**：新增 `docs/manual-checks-governance.md`，明确 `project_logs/manual_checks/` 中长期保留材料与短期临时排查文件的分类、命名约定、清理流程和保留/清理判定规则；同时在 `docs/next-phase-optimization-todos.md` 中将 TODO 6 标记为已完成并关联该文档。
- **涉及模块**：`docs/manual-checks-governance.md`、`docs/next-phase-optimization-todos.md`
- **实施方法**：不改业务代码，仅对现有手工验证与临时调试文件的使用方式做文档化收口，形成后续统一执行标准。

#### 验证标准
- **成功指标**：临时脚本管理规则被正式文档化，TODO 6 在计划文档中完成标记。
- **测试方案**：文档类收口，无需额外业务测试；通过检查文档与当前目录使用场景的一致性进行验证。
- **风险评估**：低。此次为治理规则沉淀，不影响运行逻辑。

#### 后续建议
- **相关影响**：后续执行浏览器测试、联调排查、手工验收记录时，可统一按本约定区分长期材料与临时产物。
- **待办事项**：后续如新增长期保留的手工验证材料，优先采用 `taskXX_*` 命名方式。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 18:15:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 7，进一步收口 taxonomy / media / post 路由边界，避免公共响应结构和重复辅助逻辑继续散落在多个 route 文件中。

#### 变更内容
- **具体修改**：
  - 新增 `app/schemas/api_response.py`，统一 `ApiResponse`、`ok_response()`、`error_response()` 响应结构定义。
  - `api_v1_posts.py` 与 `api_v1_taxonomy.py`、`api_v1_media.py` 改为复用统一响应结构，减少跨 route 文件相互依赖和重复实现。
  - `api_v1_posts.py` 继续复用 `admin_post_service.py` 中的默认分类与标签查询辅助逻辑。
  - `api_v1_taxonomy.py` 复用分类/标签存在性判断逻辑，保持 taxonomy 能力留在 taxonomy 路由边界内。
  - 在 `docs/next-phase-optimization-todos.md` 中将 TODO 7 标记为已完成。
- **涉及模块**：`app/schemas/api_response.py`、`app/routes/api_v1_posts.py`、`app/routes/api_v1_taxonomy.py`、`app/routes/api_v1_media.py`、`app/services/admin_post_service.py`、`docs/next-phase-optimization-todos.md`
- **实施方法**：采用最小边界收口策略，不调整现有 API 协议，仅抽取共享响应结构并清理跨 route 依赖，让 taxonomy / media / post 各自职责更清晰。

#### 验证标准
- **成功指标**：taxonomy / media / post 路由边界更清晰，回归测试和前端构建继续通过。
- **测试方案**：运行 `tests/test_api_v1_posts.py`、`tests/test_api_v1_author.py`、`tests/test_start_fullstack_script.py`，并执行前端 `npm run build`。
- **风险评估**：低到中。主要风险在于共享响应结构替换后导入关系或返回值回退；已通过回归测试与构建验证覆盖。

#### 后续建议
- **相关影响**：后续若新增 taxonomy / media / post 能力，应优先复用当前共享响应结构与各自 service，避免再次出现 route 间相互引用。
- **待办事项**：TODO 8、TODO 9 可继续在不扩张复杂度的前提下推进。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 18:35:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 8，收口主题与扩展机制的边界说明，避免在没有真实需求的情况下将现有轻量扩展入口升级为复杂插件系统。

#### 变更内容
- **具体修改**：新增 `docs/lightweight-extension-boundaries.md`，明确主题系统、扩展加载机制、事件总线（发布订阅）、数据库 provider 的当前能力、边界与扩展约束；同时在 `docs/next-phase-optimization-todos.md` 中将 TODO 8 标记为已完成并关联该文档。
- **涉及模块**：`docs/lightweight-extension-boundaries.md`、`docs/next-phase-optimization-todos.md`
- **实施方法**：不扩展功能实现，仅对现有扩展入口做边界文档化收口，确保后续开发继续保持轻量与克制。

#### 验证标准
- **成功指标**：主题与扩展机制的边界被正式文档化，TODO 8 在计划文档中完成标记。
- **测试方案**：文档类收口，无需额外业务测试；通过核对当前实现与文档边界的一致性验证。
- **风险评估**：低。此次不改变运行逻辑，仅沉淀边界说明。

#### 后续建议
- **相关影响**：后续若新增主题或扩展需求，可直接引用该文档判断是否超出当前轻量边界。
- **待办事项**：TODO 9 可继续补充文档收口，但应避免扩张为大而全说明文档。

### 【优化日志条目】

**变更类型**：优化

**时间戳**：2026-03-12 18:50:00

**负责人**：Claude

---

#### 变更目的
完成后续优化 TODO 9，对已有开发与测试文档做收口补充，避免继续增加大而全的新文档，同时让开发启动约定与浏览器冒烟测试说明形成闭环。

#### 变更内容
- **具体修改**：
  - 在 `docs/dev-fullstack-contract.md` 中补充浏览器冒烟测试基线，明确推荐保留和复用的关键路径，以及执行与清理约定。
  - 保持文档收口在现有两份文档内完成，不新增无边界的大型说明文档。
  - 在 `docs/next-phase-optimization-todos.md` 中将 TODO 9 标记为已完成，并关联 `docs/dev-fullstack-contract.md` 与 `docs/manual-checks-governance.md`。
- **涉及模块**：`docs/dev-fullstack-contract.md`、`docs/manual-checks-governance.md`、`docs/next-phase-optimization-todos.md`
- **实施方法**：只对已有文档做补充收口，不扩张文档体系，确保开发启动约定、关键运行路径与浏览器测试说明在有限文档中闭环。

#### 验证标准
- **成功指标**：开发启动约定、关键运行路径、浏览器冒烟测试说明已在现有文档中补齐，TODO 9 在计划文档中完成标记。
- **测试方案**：文档类收口，无需新增业务测试；通过核对现有文档覆盖范围与 TODO 9 目标一致性进行验证。
- **风险评估**：低。此次仅做文档收口，不改运行逻辑。

#### 后续建议
- **相关影响**：后续若继续补文档，优先在现有收口文档内扩展，避免再次引入大而全的新文档。
- **待办事项**：如未来浏览器冒烟测试脚本沉淀为正式回归入口，可在 `dev-fullstack-contract.md` 中补充统一执行命令。

---
✅ 变更已记录至 `project_logs/optimization/2026-03-08.md`
