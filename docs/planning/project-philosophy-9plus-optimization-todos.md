# 项目哲学 9.0+ 优化执行清单

日期：2026-03-14

## 1. 文档目标

本计划文档用于把“项目哲学评分从 8.2 提升到 9.0+”的路线图拆解为可执行 Todo 清单。

优化目标遵循项目核心哲学：

- 轻量
- 克制
- 可扩展

原则上：

> 不通过增加功能和抽象来提分，而通过降低噪音、收紧边界、固化扩展契约与测试来提分。

---

## 2. 评分提升目标

当前评估：

- 轻量：8.5/10
- 克制：8/10
- 可扩展：8/10
- 综合：8.2/10

目标状态：

- 轻量：8.8+
- 克制：9.0+
- 可扩展：8.8+
- 综合：9.0+

达成路径：

1. 先做“减噪收口”
2. 再做“边界固化”
3. 最后做“优雅收束”

---

## 3. 执行原则

### 3.1 轻量
- 优先删除噪音、统一入口、清理残留。
- 不引入新的基础设施、重型状态管理或平台型抽象。

### 3.2 克制
- 所有改动必须能说明其与当前边界直接相关。
- 不借优化之名做审美式重构。
- 历史材料归档，不让历史方案继续充当当前真相。

### 3.3 可扩展
- 保持已有扩展点最小可用：database provider、extension loader、hook bus、theme register。
- 补契约、补测试，不扩平台。

---

## 4. P0：立刻执行的高性价比任务

这些任务优先级最高，改动风险低，但对评分提升最直接。

## Task P0-1：清理历史文档噪音并标记过期方案
**状态：已完成**
**目标维度**：克制 / 轻量

### 目标
明确“当前真相文档”和“历史设计档案”的边界，避免新人或后续开发者误把历史方案当现行方案。

### 要做的事
- [x] 梳理 `docs/plans/` 中已明显过期的设计文档
- [x] 在过期文档顶部增加醒目标记，例如“历史方案 / 已过期 / 仅供背景参考”
- [x] 对仍有参考价值但不再代表现状的文档补一句“不要作为当前实现依据”
- [x] 检查是否存在与当前架构明显冲突的旧描述（如早期 SSR / Jinja2 / PostgreSQL 基线）

### 重点关注文件
- `docs/plans/2026-03-08-blog-design.md`
- `docs/plans/*.md`

### 验收标准
- `docs/plans/` 中不再存在容易被误认为当前真相的历史方案
- 新人能快速区分“现行规范”和“历史背景”

### 预期收益
- 降低认知噪音
- 强化边界感
- 提升“克制”评分

---

## Task P0-2：补一份 docs 总入口，统一真相源与阅读顺序
**状态：已完成**
**目标维度**：克制 / 可扩展

### 目标
建立一个简洁的文档导航入口，让读者知道：

1. 哪些文档是当前真相
2. 哪些文档是背景材料
3. 推荐按什么顺序阅读

### 要做的事
- [x] 在 `docs/` 下新增总入口文档（建议：`docs/README.md` 或 `docs/index.md`）
- [x] 按类别归档文档：product / architecture / api / engineering / planning / plans
- [x] 明确“现行规范”与“历史档案”的区别
- [x] 给出推荐阅读顺序
- [x] 修正根目录 `CLAUDE.md` 中的过期文档路径引用

### 重点关注文件
- `CLAUDE.md`
- `docs/product/product-overview.md`
- `docs/product/v1-scope.md`
- `docs/architecture/architecture-and-boundaries.md`
- `docs/api/api-v1-contract.md`
- `docs/engineering/development-guide.md`

### 验收标准
- 开发者打开一个入口文档，就能知道该先读什么
- `CLAUDE.md` 中不存在失效文档路径
- 文档真相源清晰，不再散落

### 预期收益
- 提升文档可用性
- 减少理解分叉
- 提升“克制”评分

---

## Task P0-3：清理登录与手工检查链路的调试残留
**状态：已完成**
**目标维度**：轻量 / 克制

### 目标
清理运行期长期存在的调试输出与一次性排障残留，确保主链路保持干净。

### 要做的事
- [x] 排查登录链路是否仍默认写入调试日志
- [x] 检查 `project_logs/manual_checks/` 下哪些内容属于长期材料，哪些属于临时排障残留
- [x] 将临时排障方式替换为更可控的机制：标准日志、测试、可开关诊断
- [x] 补充手工检查治理文档，明确“什么可以长期保留、什么必须清理”

### 重点关注文件
- `app/routes/api_v1_auth.py`
- `project_logs/manual_checks/auth_login_debug.log`
- `docs/engineering/manual-checks-governance.md`

### 验收标准
- 登录链路默认不再产生无明确治理的调试文件
- `project_logs/manual_checks/` 只保留明确归档价值的材料
- 临时排障产物能被及时清理

### 预期收益
- 提升运行链路洁净度
- 提升“轻量”评分

---

## 5. P1：将哲学固化为机制

这些任务在 P0 完成后执行，用来把项目哲学从“共识”固化为“规则”。

## Task P1-1：建立变更边界检查清单
**状态：已完成**
**目标维度**：克制

### 目标
为后续新增需求、修复和优化提供统一判断标准，防止项目边界再次漂移。

### 要做的事
- [x] 增加一份变更边界检查清单文档
- [x] 明确每次新增改动需要回答的问题：
  - [x] 是否突破 V1 范围？
  - [x] 是否引入了非必要依赖？
  - [x] 是否让一个已有模块开始承担平台职责？
  - [x] 是否增加运行态复杂度？
  - [x] 是否破坏默认 SQLite / Session / 单体基线？
  - [x] 是否属于“为未来假设提前设计”？
- [x] 把检查清单链接进工程文档或 CLAUDE 指南中

### 建议位置
- `docs/engineering/change-review-checklist.md`
或
- `docs/engineering/evolution-guardrails.md`

### 验收标准
- 后续任何较大变更都可套用同一检查清单
- 项目哲学具备可执行的审查标准

### 预期收益
- 明显提升“克制”评分
- 降低功能边界失控风险

---

## Task P1-2：补强扩展点的最小契约说明
**状态：已完成**
**目标维度**：可扩展 / 克制

### 目标
把已有扩展点从“代码上可扩”提升到“边界上清晰可扩”。

### 要做的事
- [x] 为 `database_provider` 补充责任边界与支持范围说明
- [x] 为 `extension_loader` 补充加载时机、允许行为、不允许行为说明
- [x] 为 `hook_bus` 补充事件命名、payload 结构、异常处理约束说明
- [x] 为 `ThemeProvider` 补充主题注册约束与非目标说明
- [x] 统一这些说明与 `docs/api/extension-api-v1.md`、`docs/architecture/lightweight-extension-boundaries.md`

### 重点关注文件
- `app/core/database_provider.py`
- `app/core/extension_loader.py`
- `app/core/hook_bus.py`
- `frontend/src/shared/theme/ThemeProvider.jsx`
- `docs/api/extension-api-v1.md`
- `docs/architecture/lightweight-extension-boundaries.md`

### 验收标准
- 扩展点的“能做什么 / 不能做什么”在文档中清晰可查
- 不再需要通过猜代码理解扩展边界

### 预期收益
- 提升“可扩展”评分
- 防止扩展点被误用成平台化入口

---

## Task P1-3：为扩展边界补针对性测试
**状态：已完成**
**目标维度**：可扩展 / 轻量

### 目标
通过边界测试固定最小扩展机制的行为，避免未来演进中悄悄变形。

### 要做的事
- [x] 为 `hook_bus` 补测试
  - [x] 监听器异常不影响主流程
  - [x] 多监听器都可执行
  - [x] 未注册事件正常处理
- [x] 为 `extension_loader` 补测试
  - [x] 空配置可正常启动
  - [x] 扩展导入失败时行为明确
  - [x] 样例扩展可成功加载
- [x] 为 `database_provider` 补测试
  - [x] SQLite URL 解析
  - [x] PostgreSQL URL 解析
  - [x] 非支持 provider 的失败路径
- [x] 必要时补前端主题注册的最小测试或文档化验证步骤

### 重点关注文件
- `tests/test_hook_bus.py`
- `tests/test_extension_loader.py`
- `tests/test_database_provider.py`
- `frontend/src/shared/theme/ThemeProvider.jsx`

### 验收标准
- 核心扩展点均有边界测试覆盖
- 扩展能力的最小承诺被自动化验证固定下来

### 预期收益
- 提升“可扩展”稳定性
- 降低未来演进回归风险

---

## Task P1-4：给热点模块设置职责红线
**状态：已完成**
**目标维度**：轻量 / 克制

### 目标
阻止文章管理与后台页面继续自然膨胀，提前建立职责边界。

### 要做的事
- [x] 为 `app/routes/api_v1_posts.py` 明确“路由层只负责什么”
- [x] 为 `frontend/src/admin/pages/AdminPostsPage.jsx` 明确“页面层只负责什么”
- [x] 若后续继续膨胀，只允许做最小拆分，不做大重构
- [x] 在相关工程文档中记录“热点模块收口原则”

### 重点关注文件
- `app/routes/api_v1_posts.py`
- `frontend/src/admin/pages/AdminPostsPage.jsx`
- `frontend/src/admin/hooks/useAdminPostsState.js`

### 验收标准
- 新增需求不会直接无脑堆到热点文件
- 路由 / 页面 / hook / service 的边界更加稳定

### 预期收益
- 提升长期可维护性
- 防止后续分数回落

---

## 6. P2：长期保持 9.0+ 的收束任务

这些任务不是为了立刻提分，而是为了防止项目再次变重。

## Task P2-1：对热点文件做小幅收口拆分
**状态：已完成**
**目标维度**：轻量 / 克制

### 目标
只在确有必要时，对最容易继续膨胀的热点文件做最小收口。

### 要做的事
- [x] 评估 `app/routes/api_v1_posts.py` 是否存在非路由逻辑继续外溢的问题
- [x] 评估 `AdminPostsPage` / `useAdminPostsState` 是否需要进一步局部下沉逻辑
- [x] 仅做最小拆分，避免引入新抽象体系

### 验收标准
- 热点文件复杂度不再继续线性增长
- 没有因为收口而引入新的层级负担

---

## Task P2-2：减少“同一事实多处定义”
**状态：已完成**
**目标维度**：克制 / 可维护性

### 目标
让关键规则尽可能单点定义，避免文档和代码多处漂移。

### 要做的事
- [x] 检查错误码是否存在重复定义
- [x] 检查初始化规则是否只有一个权威来源
- [x] 检查图片类型支持是否单点定义
- [x] 检查 taxonomy 相关字段含义是否在代码与文档中一致
- [x] 检查 API 字段说明是否需要统一收口

### 验收标准
- 同一规则尽量只有一个权威来源
- 文档与实现同步成本降低

---

## Task P2-3：统一日常开发入口与维护路径
**状态：已完成**
**目标维度**：轻量

### 目标
让开发、联调、测试的主路径长期保持少而稳定。

### 要做的事
- [x] 明确推荐使用 `python scripts/start_blog.py`
- [x] 明确推荐使用 `python scripts/start_fullstack.py`
- [x] 明确 `pytest` 为基础验证入口
- [x] 检查是否存在重复、重叠、长期少维护的脚本入口
- [x] 更新开发文档，减少“多个入口多种说法”

### 重点关注文件
- `scripts/start_blog.py`
- `scripts/start_fullstack.py`
- `scripts/run_dev.py`
- `docs/engineering/development-guide.md`
- `docs/engineering/dev-fullstack-contract.md`

### 验收标准
- 新人能按最少命令完成开发启动和测试
- 日常维护路径简洁稳定

---

## 7. 建议执行顺序

按最推荐顺序执行：

1. [ ] Task P0-1：清理历史文档噪音并标记过期方案
2. [ ] Task P0-2：补一份 docs 总入口，统一真相源与阅读顺序
3. [ ] Task P0-3：清理登录与手工检查链路的调试残留
4. [ ] Task P1-1：建立变更边界检查清单
5. [ ] Task P1-2：补强扩展点的最小契约说明
6. [ ] Task P1-3：为扩展边界补针对性测试
7. [ ] Task P1-4：给热点模块设置职责红线
8. [ ] Task P2-1：对热点文件做小幅收口拆分
9. [ ] Task P2-2：减少“同一事实多处定义”
10. [ ] Task P2-3：统一日常开发入口与维护路径

---

## 8. 哪些不要做

为了把分数稳定提升到 9.0+，以下方向当前不建议做：

- [ ] 不要把扩展机制升级为插件平台
- [ ] 不要为了“前端规范化”引入重型全局状态管理
- [ ] 不要为了数据库抽象继续下挖平台化适配层
- [ ] 不要借优化之名做大范围审美式重构
- [ ] 不要扩评论、多用户、多角色等超出当前边界的能力
- [ ] 不要继续增加大而全但缺乏维护的文档

---

## 9. 9.0+ 达标判定

当以下条件大部分满足时，可认为项目已接近或达到 9.0+：

### 文档层
- [ ] 当前真相文档清晰
- [ ] 历史文档不再混淆现状
- [ ] 扩展边界文档完整且稳定

### 代码层
- [ ] 主链路无明显调试残留
- [ ] 热点模块未继续膨胀
- [ ] 关键规则尽量单点定义

### 测试层
- [ ] 初始化 / 认证 / 文章 / taxonomy 主链路稳定
- [ ] 扩展边界有针对性测试

### 演进层
- [ ] 后续变更可套用边界检查清单
- [ ] 项目未因“看起来更强大”而自然平台化

---

## 10. 冲 9.5+ 的后续 Todo 清单

当前计划已完成 P0 / P1 / P2，项目综合评分已达到约 9.1。

若要继续冲到 9.5+，重点不再是新增功能，而是继续强化：

- 热点模块可读性
- 关键规则单点定义
- 回归护栏测试
- 文档长期治理机制

---

## P3：立刻做，继续提分

这些任务延续当前优化方向，成本低、收益高。

### TODO P3-1：继续收口 `api_v1_posts.py`
**状态：已完成**
**目标维度**：轻量 / 克制

#### 目标
把文章路由进一步压缩成“路由编排层”，减少残余重复与隐性复杂度。

#### 要做的事
- [x] 检查 `create_admin_post_api()` 与 `import_markdown_post_api()` 是否还能继续共用更薄的创建路径
- [x] 检查 admin 列表筛选逻辑是否可以抽成轻量 helper
- [x] 检查 export / publish / unpublish 是否还有可合并的局部重复
- [x] 保持单文件，不新增多层架构

#### 验收标准
- 文章路由更像“参数接收 + 调 service/helper + 返回响应”
- 文件长度不是唯一目标，重点是认知负担下降
- 不引入 controller/usecase/repository 等新层

---

### TODO P3-2：继续收口 `useAdminPostsState.js`
**状态：已完成**
**目标维度**：轻量 / 可维护性

#### 目标
让后台文章 hook 更像“页面状态编排器”，减少长函数堆积。

#### 要做的事
- [x] 将 `submitForm`、`importMarkdown`、`uploadImage`、`togglePublish` 中可提取的纯转换逻辑收口为局部 helper
- [x] 让 hook 中“数据转换”和“副作用调用”边界更清晰
- [x] 保持当前 hook 结构，不引入全局状态管理

#### 验收标准
- hook 仍然是单入口
- 长逻辑段更少
- 更容易区分“状态”“转换”“副作用”

---

### TODO P3-3：建立错误码单点定义
**状态：已完成**
**目标维度**：克制 / 单点定义

#### 目标
让错误码不再以裸数字形式分散在多个 route / 文档里。

#### 要做的事
- [x] 新建错误码权威定义位置（如 `app/core/error_codes.py`）
- [x] 将核心错误码集中定义：`1001`、`1002`、`1003`、`1404`、`1409`、`1410`、`1411`、`1412`、`1413`、`1414`
- [x] route 改为引用常量而不是裸数字
- [x] 文档更新为以该定义为权威来源

#### 验收标准
- 错误码不再散落
- route 可读性提升
- API 文档与实现更不容易漂移

---

### TODO P3-4：继续收口媒体规则定义
**状态：已完成**
**目标维度**：克制 / 单点定义

#### 目标
让媒体上传相关规则形成更完整的小型权威定义区。

#### 要做的事
- [x] 检查图片大小限制、MIME 类型、扩展名映射是否还能进一步集中
- [x] 如有必要，将 MIME 类型、扩展名映射、大小限制继续收口到同一处
- [x] 文档明确以该处为权威来源

#### 验收标准
- 图片规则更集中
- 不再出现“代码和文档各写一套”的趋势

---

## P4：中期做，把 9.3 往 9.5 推

这些任务更偏“长期稳态护栏”。

### TODO P4-1：补“哲学冻结规则”测试
**状态：已完成**
**目标维度**：轻量 / 克制 / 可扩展

#### 目标
不仅测功能，还测项目哲学当前冻结下来的关键约束。

#### 要做的事
- [x] 补测试验证默认数据库仍为 SQLite 基线
- [x] 补测试验证登录接口仍为 form 提交契约
- [x] 补测试验证 API 响应结构仍统一为 `code/message/data`
- [x] 补测试验证开发主入口仍为 `start_blog.py` / `start_fullstack.py` 的文档约束对应实现
- [x] 补测试验证扩展点仍保持当前最小行为边界

#### 验收标准
- 测试不只是测“能不能跑”
- 还测“会不会悄悄偏离哲学基线”

---

### TODO P4-2：建立核心回归测试包
**状态：已完成**
**目标维度**：稳定性 / 可维护性

#### 目标
明确哪些测试是项目的核心护栏，便于后续持续回归。

#### 要做的事
- [x] 整理一组核心回归测试范围：初始化、认证、文章 CRUD / 发布、taxonomy、图片上传、扩展边界、启动脚本
- [x] 在文档中明确这些测试是高价值回归集合
- [x] 如有必要，约定后续优先跑哪些测试文件

#### 验收标准
- 后续改动有一套明确的核心回归范围
- 不再每次都靠人工判断“该测哪些”

---

### TODO P4-3：给文档增加维护规则
**状态：已完成**
**目标维度**：克制 / 文档治理

#### 目标
防止未来又重新出现平行真相源。

#### 要做的事
- [x] 在 `docs/README.md` 或工程文档中明确：哪些改动必须同步更新文档
- [x] 明确哪些文档属于当前真相，哪些文档只能记录背景，不承载现行规范
- [x] 明确 `planning/` 与 `plans/` 的职责差异
- [x] 明确新增文档前先判断是否会制造新的维护负担

#### 验收标准
- 新文档不会轻易把真相源再次打散
- 历史档案与现行规范长期分层稳定

---

### TODO P4-4：建立关键规则权威来源索引
**状态：已完成**
**目标维度**：克制 / 可维护性

#### 目标
建立一份“关键规则权威来源索引”。

#### 要做的事
- [x] 明确以下规则的权威来源：错误码、初始化状态判定、API 响应结构、图片上传限制、扩展边界、开发入口
- [x] 把这些来源整理进文档入口或工程规范

#### 验收标准
- 开发者能快速知道“改某类规则该去哪改”
- 不再靠记忆找权威文件

---

## P5：长期保持，冲 9.5+ 后不回落

这些任务不一定马上做，但决定能否真正稳住 9.5+。

### TODO P5-1：每次新增需求都先过边界检查清单
**状态：已完成（已固化为长期守护规则）**

#### 要做的事
- [x] 后续所有中型改动前，先过 `change-review-checklist.md`
- [x] 发现边界不清时，先补文档再改代码
- [x] 发现热点膨胀时，优先做最小收口

---

### TODO P5-2：持续监控热点文件反弹
**状态：已完成（已固化为长期守护规则）**

#### 重点对象
- [x] `app/routes/api_v1_posts.py`
- [x] `frontend/src/admin/hooks/useAdminPostsState.js`
- [x] 认证相关 shared 模块
- [x] 扩展点相关 core 模块

---

### TODO P5-3：控制文档体积与噪音增长
**状态：已完成（已固化为长期守护规则）**

#### 要做的事
- [x] 新增文档前先判断是否已有文档可补充
- [x] 避免写一份和现有内容重叠的新文档
- [x] 定期检查历史文档是否仍需显式标注状态

---

## 11. 9.5+ 达标判定

### 文档层
- [ ] 当前真相、规划记录、历史档案完全分层稳定
- [ ] 新增文档不会再制造平行真相
- [ ] 关键规则权威来源清晰

### 代码层
- [ ] 热点模块复杂度继续下降
- [ ] 关键规则基本都单点定义
- [ ] 没有明显重复逻辑或隐性平台化趋势

### 测试层
- [ ] 核心功能测试稳定
- [ ] 边界测试稳定
- [ ] 哲学冻结规则测试稳定

### 演进层
- [ ] 新需求来了仍然能保持轻量、克制、可扩展
- [ ] 项目不会因为“顺手做一点”慢慢重新变重

---

## 13. 冲过 9.5 的最后一公里清单

### 目标
最后一公里阶段，只做三类事：

- 继续压平热点复杂度
- 继续清除残余平行规则
- 继续把治理闭环补到“几乎无短板”

### 第一优先级：最可能直接把分数推过 9.5
- [x] 继续压平 `app/routes/api_v1_posts.py`
- [x] 继续压平 `useAdminPostsState.js`

### 第二优先级：继续清除剩余平行实现
- [x] 统一 `api_v1_setup.py` 的 `_ok/_error` 与 setup 错误码
- [x] 再检查 author / setup / taxonomy 是否还有少量平行规则
- [x] 把 `rule-authority-index.md` 再补完整一点

### 第三优先级：把高分状态锁住
- [x] 给哲学冻结规则测试再补两三个最关键的断言
- [x] 把核心回归包缩成一套“最小高价值命令”

### 建议执行顺序
1. [ ] 继续收口 `app/routes/api_v1_posts.py`
2. [ ] 继续收口 `useAdminPostsState.js`
3. [ ] 统一 `api_v1_setup.py` 的 `_ok/_error` 与 setup 错误码来源
4. [ ] 再检查 author / taxonomy / setup 是否还有局部平行规则
5. [ ] 补全 `rule-authority-index.md`
6. [ ] 补几条最关键的哲学冻结规则测试
7. [ ] 把核心回归包再压成最小高价值命令

---

## 14. 结论

当前已经完成 P0 / P1 / P2 / P3 / P4 / P5。若继续完成“最后一公里”中的热点压平、setup 统一与高价值护栏补强，项目更有机会稳定跨过 9.5+。

---

## 15. 冲 9.5+ 的整改清单（按优先级）

### 目标

本节只保留当前仍未完成、且最直接面向 9.5+ 的收口任务。

继续提分的方式不是加功能，而是继续：

- [ ] 收口热点模块
- [ ] 统一真相来源
- [ ] 补高价值护栏测试
- [ ] 稳固长期治理边界

统一约束如下：

- [ ] 不新增平台化抽象
- [ ] 不引入重型状态管理
- [ ] 不借提分做审美式重构
- [ ] 不新建与本主题并行的 planning 文档

---

### P1：最高优先级，最直接影响 9.5+

#### Task P1-1：继续收口 `app/routes/api_v1_posts.py`
- [x] **目标维度**：轻量 / 克制
- [x] **为什么现在做**：`app/routes/api_v1_posts.py` 仍是文章主链路热点文件。当前阶段继续提分，不是追求文件更短，而是继续把它压回“路由编排层”，降低未来再次反弹成万能中心的风险。
- [x] **具体要做的事**
  - [x] 继续检查 admin 与 public 路由中是否仍有残余参数归一化、筛选拼装、响应拼装逻辑可收口
  - [x] 优先复用现有 service / helper，不新增 controller / usecase / repository 等新层
  - [x] 若存在局部重复，只做薄收口，不做跨模块大重构
  - [x] 明确哪些逻辑必须留在 route，哪些逻辑应继续下沉到现有 service/helper
- [x] **涉及文件**
  - [x] `app/routes/api_v1_posts.py`
  - [x] `app/services/post_service.py`
  - [x] `app/services/admin_post_service.py`
  - [x] `docs/engineering/evolution-guardrails.md`
- [x] **验收标准**
  - [x] `api_v1_posts.py` 更稳定承担“参数接收 + 调用 service/helper + 返回响应”的职责
  - [x] 相同规则不再在多个路由分支里继续平行堆积
  - [x] 未引入新的平台化抽象层
  - [x] 热点模块认知负担继续下降，而不是只追求文件拆分数量
- [x] **不要做什么**
  - [x] 不为“整洁”拆出一套多层架构
  - [x] 不把局部收口升级为通用文章平台改造
  - [x] 不新增脱离当前博客闭环的流程能力

---

#### Task P1-2：统一前端认证表达，降低 localStorage / session 的“双真相感”
- [x] **目标维度**：克制 / 轻量
- [x] **为什么现在做**：当前认证机制的真实依据已经明确是后端 session，但前端仍容易给人一种“localStorage 也像真相源”的感觉。要继续冲 9.5+，需要把这种表达继续收口，避免后续围绕假状态继续长出额外逻辑。
- [x] **具体要做的事**
  - [x] 继续检查 shared auth 模块、登录页、`AuthGuard`、后台入口页是否存在把本地缓存当作真实登录态的表达
  - [x] 统一未授权后的最小收敛动作：清理本地缓存、跳转登录页、停止派生更多前端认证语义
  - [x] 若存在重复失效处理逻辑，优先收口到现有 shared auth 边界内
  - [x] 保持“前端只做体验层同步，后端 session 才是真认证”这一表达在代码与文档中的一致性
- [x] **涉及文件**
  - [x] `frontend/src/shared/auth/`
  - [x] `frontend/src/admin/pages/AdminLoginPage.jsx`
  - [x] `frontend/src/shared/auth/AuthGuard` 相关文件
  - [x] `docs/engineering/evolution-guardrails.md`
  - [x] `docs/engineering/long-term-guardrails.md`
- [x] **验收标准**
  - [x] 前端认证相关代码不再暗示 localStorage 是并列真相源
  - [x] 未授权处理路径更统一，重复逻辑更少
  - [x] 没有引入 AuthContext 状态机、session restore 流程或其他更重方案
  - [x] 文档与代码都继续强调“session 是唯一真实认证依据”
- [x] **不要做什么**
  - [x] 不引入全局认证状态平台
  - [x] 不新增复杂登录态恢复机制
  - [x] 不把轻量 session 认证补成前端主导的认证框架

---

### P2：第二优先级，补边界与单点定义

#### Task P2-1：继续收口 `frontend/src/admin/hooks/useAdminPostsState.js`
- [x] **目标维度**：轻量 / 可扩展
- [x] **为什么现在做**：该 hook 已经承担后台文章页的大部分状态编排，是前端侧长期高风险热点。若不继续收口，后续小需求最容易继续顺手堆进这里。
- [x] **具体要做的事**
  - [x] 继续检查数据转换、表单归一化、副作用调用之间是否还有残余混杂段落
  - [x] 仅提取局部纯函数或局部 helper，保持 hook 仍是单入口编排层
  - [x] 让页面层、hook 层、子组件层职责边界更稳定
  - [x] 对新增逻辑优先复用现有结构，不额外引入全局 store
- [x] **涉及文件**
  - [x] `frontend/src/admin/hooks/useAdminPostsState.js`
  - [x] `frontend/src/admin/pages/AdminPostsPage.jsx`
  - [x] `frontend/src/admin/components/`
  - [x] `docs/engineering/evolution-guardrails.md`
- [x] **验收标准**
  - [x] hook 中“状态管理 / 数据转换 / 副作用调用”边界更容易区分
  - [x] 新增逻辑不再优先堆进长函数
  - [x] 页面与 hook 的职责更稳定
  - [x] 未引入 Redux、zustand、全局上下文状态机等重型方案
- [x] **不要做什么**
  - [x] 不把局部状态编排升级成全局状态管理体系
  - [x] 不为了形式统一做大面积前端重构
  - [x] 不新增脱离当前页面真实需求的抽象层

---

#### Task P2-2：固化 `SiteSettings` 职责边界，避免继续膨胀
- [x] **目标维度**：克制 / 可扩展
- [x] **为什么现在做**：`SiteSettings` 很容易因为“先放这里最省事”而继续吸收无关配置，最终变成杂项总表。要冲 9.5+，需要提前把它的职责边界继续写实并锁住。
- [x] **具体要做的事**
  - [x] 重新检查 `SiteSettings` 当前承载的字段是否仍然都属于站点级基础设置
  - [x] 明确哪些内容可以继续归入站点设置，哪些内容应禁止顺手塞入
  - [x] 如存在边界模糊处，优先补文档与测试约束，而不是先扩字段
  - [x] 对后续新增字段请求增加“是否属于站点基础设置”的判断标准
- [x] **涉及文件**
  - [x] `app/models/site_settings.py`
  - [x] `app/routes/api_v1_author.py`
  - [x] `app/routes/api_v1_setup.py`
  - [x] `docs/architecture/architecture-and-boundaries.md`
  - [x] `docs/engineering/long-term-guardrails.md`
- [x] **验收标准**
  - [x] `SiteSettings` 的职责边界描述更清晰
  - [x] 新增字段不会再默认落入“先塞 SiteSettings”路径
  - [x] setup / author / site settings 的边界理解更一致
  - [x] 未把站点设置扩成通用配置中心
- [x] **不要做什么**
  - [x] 不把 `SiteSettings` 演进成大而全配置平台
  - [x] 不为了未来假设预留大量空字段
  - [x] 不顺手引入新的配置分层体系

---

#### Task P2-3：继续压缩同一规则多处定义
- [x] **目标维度**：克制 / 可维护性
- [x] **为什么现在做**：当前项目已经完成一轮单点定义治理，但 setup / author / taxonomy 等链路仍可能残留少量平行规则。继续冲高分，关键是把这些残余热点再压一轮，而不是新增更多治理口号。
- [x] **具体要做的事**
  - [x] 继续检查 setup / author / taxonomy 相关规则是否仍存在代码与文档双处漂移风险
  - [x] 优先补充或修正权威来源索引，让开发者知道该去哪改
  - [x] 若发现平行规则，优先收口到已有权威定义，不新建第二套“临时正确版本”
  - [x] 把残余的重复错误码、响应包装、边界说明继续压回单点来源
- [x] **涉及文件**
  - [x] `app/routes/api_v1_setup.py`
  - [x] `app/routes/api_v1_author.py`
  - [x] `app/routes/api_v1_taxonomy.py`
  - [x] `docs/engineering/rule-authority-index.md`
  - [x] `docs/api/api-v1-contract.md`
- [x] **验收标准**
  - [x] setup / author / taxonomy 残余平行规则进一步减少
  - [x] 关键规则来源更可追踪
  - [x] 开发者不需要在多个文件间猜哪一处才是当前真相
  - [x] 未新增新的并行权威文档
- [x] **不要做什么**
  - [x] 不为解决少量重复而做跨项目级大重构
  - [x] 不把局部规则整理升级成新的平台规范系统
  - [x] 不新建同主题文档与现有真相源并列存在

---

### P3：第三优先级，锁住长期 9.5+

#### Task P3-1：增补“哲学冻结规则”与高价值回归护栏
- [x] **目标维度**：轻量 / 克制 / 可扩展
- [x] **为什么现在做**：9.5+ 不只是一次性清理结果，更要靠护栏长期防回弹。当前最值得补的不是更多功能测试，而是最能锁住项目哲学基线的少量高价值断言。
- [x] **具体要做的事**
  - [x] 检查现有 `test_philosophy_guardrails` 与核心回归集合，补最能防止边界回退的断言
  - [x] 优先覆盖默认基线、认证契约、统一响应结构、热点边界、扩展边界等关键冻结点
  - [x] 保持测试集合小而硬，不把所有测试都塞进核心护栏
  - [x] 在工程文档中继续明确何时优先跑这些高价值回归
- [x] **涉及文件**
  - [x] `tests/test_philosophy_guardrails.py`
  - [x] `docs/engineering/core-regression-suite.md`
  - [x] `docs/engineering/long-term-guardrails.md`
- [x] **验收标准**
  - [x] 核心哲学基线有更稳定的自动化护栏
  - [x] 高价值回归集合仍保持克制，不膨胀成大而全清单
  - [x] 后续中型改动更容易快速验证是否越界
  - [x] 测试新增聚焦长期价值，而非堆数量
- [x] **不要做什么**
  - [x] 不把所有测试都标成核心回归
  - [x] 不为了“覆盖率好看”增加低价值测试
  - [x] 不让护栏测试反过来驱动无意义实现复杂化

---

#### Task P3-2：明确热点文件长期监控对象与触发收口条件
- [x] **目标维度**：克制 / 可维护性
- [x] **为什么现在做**：只知道哪些文件是热点还不够，还需要明确“什么时候算再次膨胀、何时必须收口”，否则长期治理容易退化成凭感觉判断。
- [x] **具体要做的事**
  - [x] 继续明确长期监控对象：文章路由、文章后台 hook、shared auth、扩展 core 模块等
  - [x] 为这些热点补充更具体的触发条件，例如：职责越界、重复逻辑增加、规则来源分叉、单文件持续吸收新职责
  - [x] 把触发条件写入现有治理文档，而不是额外新建一份并行清单
  - [x] 让后续改动在进入实现前就能先判断是否需要收口
- [x] **涉及文件**
  - [x] `docs/engineering/evolution-guardrails.md`
  - [x] `docs/engineering/long-term-guardrails.md`
  - [x] `docs/engineering/change-review-checklist.md`
- [x] **验收标准**
  - [x] 热点文件监控对象清晰且稳定
  - [x] “需要收口”的判断不再主要依赖个人记忆
  - [x] 触发条件可复用到后续评审和改动自查
  - [x] 未新增文档噪音或平行治理规则
- [x] **不要做什么**
  - [x] 不做重型静态治理平台
  - [x] 不增加复杂流程审批链路
  - [x] 不把简单治理问题工程化成独立系统

---

#### Task P3-3：继续严格执行“新增文档先补旧文档”的治理要求
- [x] **目标维度**：克制 / 轻量
- [x] **为什么现在做**：文档平行真相一旦回潮，会快速拉低当前已经建立起来的治理质量。要稳定在 9.5+，必须把“先补旧文档、避免新增并行文档”继续当作长期默认动作。
- [x] **具体要做的事**
  - [x] 后续新增文档前，先判断现有真相文档是否已经可以补充
  - [x] planning 目录只保留当前仍需执行或维护的计划文档，不让同主题计划并列增长
  - [x] 若历史文档只剩背景价值，继续归档并保持状态清晰
  - [x] 对涉及产品范围、架构边界、API 契约、工程规则的改动，优先更新对应真相文档
- [x] **涉及文件**
  - [x] `docs/README.md`
  - [x] `docs/planning/project-philosophy-9plus-optimization-todos.md`
  - [x] `docs/engineering/long-term-guardrails.md`
- [x] **验收标准**
  - [x] 同主题 planning 文档不再并行增长
  - [x] 文档维护继续遵守单一真相源原则
  - [x] 读者能稳定判断“当前真相”和“历史材料”的差异
  - [x] 文档数量控制继续服务于理解效率，而不是制造噪音
- [x] **不要做什么**
  - [x] 不为了记录一次讨论就新增长期维护文档
  - [x] 不让 planning 目录再次承载平行真相
  - [x] 不把文档治理变成只增不减的堆积行为
